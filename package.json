{
  "name": "telegrambot",
  "version": "1.0.0",
  "description": "",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node bot.js",
    "build": "tsc",
    "build:dev": "tsc --watch",
    "build:prod": "tsc --watch --sourceMap false",
    "build:prod:dev": "tsc --watch --sourceMap false --outDir ./dist",
    "build:prod:dev:prod": "tsc --watch --sourceMap false --outDir ./dist --removeComments"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.9.0",
    "dotenv": "^16.5.0",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
